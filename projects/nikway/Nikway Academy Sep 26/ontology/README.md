# NIKWAY Ontology

این ontology واژگان و روابط کانونی مشترک میان NIKWAY، Agentic Army و مدل زبانی را تعریف می‌کند.

ترتیب اعتبار:

`source → evidence → fact → entity → relation → master synthesis`

فایل اصلی `ontology.yaml` است و `entity-index.yaml` فهرست سریع موجودیت‌های کانونی را ارائه می‌کند.
